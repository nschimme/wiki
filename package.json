{
  "name": "mume-wiki",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/gather-pages.js",
    "docs:dev": "npm run prebuild && vitepress dev docs",
    "docs:build": "npm run prebuild && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "migrate": "node scripts/fix-pages.js"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
